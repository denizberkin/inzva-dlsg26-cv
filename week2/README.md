# 📌 Week 2 -- Computer Vision: Segmentation & Detection

To follow the lecture material in sequence, read the markdown files in
the order listed below.

------------------------------------------------------------------------

## 1) Image Segmentation Using CNNs

### 1.1 U-Net

This section introduces semantic segmentation using convolutional neural
networks.\
U-Net is one of the most widely used architectures for segmentation
tasks, especially in medical imaging and dense prediction problems.

**Read in order:**

-   `1_IMAGE_SEGMENTATION_USING_CNNS.md`

------------------------------------------------------------------------

## 2) Object Detection Using CNNs

### 2.1 YOLO (You Only Look Once)

This section covers object detection using single-stage detectors.\
YOLO is a real-time object detection system that predicts bounding boxes
and class probabilities in one forward pass.

**Read in order:**

-   `2_OBJECT_DETECTION_USING_CNNS.md`

------------------------------------------------------------------------

### 2.1.1 Evaluation Metrics

Understanding how to evaluate object detection models:

-   Intersection over Union (IoU)
-   Precision
-   Recall
-   Average Precision (AP)
-   Mean Average Precision (mAP)

**Read:**

------------------------------------------------------------------------

### 2.1.2 YOLO Architecture

Detailed explanation of:

-   Backbone
-   Neck
-   Head
-   Grid-based prediction mechanism
-   Anchor boxes
-   Loss function

**Read:**

------------------------------------------------------------------------

## 🔎 Suggested Learning Flow

1️⃣ Image Segmentation → U-Net\
2️⃣ Object Detection → YOLO\
3️⃣ Evaluation Metrics\
4️⃣ YOLO Architecture

------------------------------------------------------------------------

This structure mirrors the Week 1 ordering style and keeps the lecture
flow consistent.
