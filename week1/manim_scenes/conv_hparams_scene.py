"""
animation on showing the hyperparameter effects on convolution ops in 2d. 
(convert to 3d mid animation if there is time left)
"""
from manim import Scene, config, BLACK, Text, MathTex

# config.background_color = "#F5F5DC"  # beige
# Text.set_default(color=BLACK)
# MathTex.set_default(color=BLACK)


class ConvHParamScene(Scene):
    def construct(self):
        pass
